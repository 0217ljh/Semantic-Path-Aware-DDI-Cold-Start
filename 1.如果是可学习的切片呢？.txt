1.如果是可学习的切片呢？
2.分子信息只应该作为指导？


* emergnn 0.72 / 
* 

结果. KG-based model 找不到 meeting node，molecular-only model 可能从 SMILES 学到 reactivity pattern
这就是 niche_frac = 0.428 那 43% pair 上 motif 比 MNAH 强的 case——分子信号有 latent value，但只在 KG-sparse niche 上 access-able。


* C1 = Pair-Mediator Pooling, 纯 architecture （离散token）
    - cold-start invariant scoring primitive over typed drug-mediator graph
    - endpoint-stripping invariant
    - AGNOSTIC to how mediators / edges are obtained
    - NO LLM involvement in C1's core claim

C2 = Mechanism-aware KG enrichment and typing
    - 包括 ALL LLM-derived contributions:
      * new role-specific edges
      * finer relation types (taxonomy)
      * optional mediator text embeddings (LLM-as-encoder)
    - 所有 LLM 用法统一在 C2 里
    - 不再人为分割成 "C1 helper" vs "C2 enrichment"

C3 = KG-Mol redundancy-aware fusion boundary（保持）




















两个层级一起 work.

Cluster path = "他们俩通过哪些 cluster 类型互动"（粗粒度 reasoning）
Within-cluster pool = "在每个 cluster 内具体是哪些 mediator"（细粒度 semantic）

这里的第二点应该对应到进一步的mechanism标签？但是怎么处理unseen？






实验日志：

V1：基本实现我们的目的，在emergnn的基础上涨3到5个点
V1.X: 修改版本如下所示
1.1 ： cluster graph：把KG 抽象成分层的cluster graph，直接在cluster graph上走
1.2： DiffPool (NeurIPS 2018) 不用自己构造18类 （学习cluster）
1.3：cluster path 联合 （meta-path）
1.4：language model init



* 纯结构方法，非GNN，极限0.79


缺陷1：只数固定的1~2hop，不会改变
缺陷2：只过MLP处理mediator embed，虽然考虑了relation，但是这个embed太多了，而且只用MLP
缺陷3：