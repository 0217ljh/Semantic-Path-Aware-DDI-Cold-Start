# HDN-DDI Baseline Review — paper-faithful (binary_cls + multi_cls)

- **Date**: 2026-05-17
- **Primary reviewer**: Claude (sonnet-4.5) — drafted this report, ran
  layout / file-independence / paper-vs-impl checks
- **Independent reviewer**: codex (gpt-5-codex) — second opinion gate, output
  copied verbatim in §6 below
- **Triggered by**: 用户要求"review baseline 下 HDN-DDI 的代码，遵循 claude 的规范"
  → followed by user instruction to migrate from flat → paper-faithful BRICS
  variant (drop _brics suffix; remove flat variant entirely)
- **Scope**: `Code/baseline/hdn_ddi/` — both `binary_cls/` and `multi_cls/` tasks
- **Reference**:
  - Paper / github: see `Code/reproductions/HDN-DDI/paper-and-github/`
  - Paper-faithful reference: `Code/reproductions/HDN-DDI/` (reviewed separately;
    see `_reviews/2026-05-17__paper_faithful.md` there)
- **CLAUDE.md regulations applied** (all sections in §"复现 → baseline 派生
  baseline 的 review 规范" cluster):
  - §"从 reproduction 派生 baseline 的 review 规范" (semantic fidelity per task)
  - §"Baseline 目录布局规范" (task-split subfolders, shared code at top)
  - §"文件级独立性" (no cross-imports, no shared files)
  - §"代码修改规则" (新增功能优先新建文件; 禁止删除/重构 unless explicit)
  - §"训练进度日志规范" + §"训练中间 eval / save 规范"

---

## 1. Read inventory

### Files in baseline/hdn_ddi/ (post-migration, 10 .py)
| File | Lines | Reviewed |
|---|---|---|
| `__init__.py` | 47 | ✓ |
| `_shared.py` | 63 | ✓ (NEW this round) |
| `layers.py` | 91 | ✓ (shared, byte-exact port of jcsun-00) |
| `models.py` | 117 | ✓ (shared) |
| `mol_features.py` | 405 | ✓ (BRICS featurizer, renamed from `_brics` suffix) |
| `binary_cls/__init__.py` | 20 | ✓ |
| `binary_cls/baseline.py` | 446 | ✓ (HDNDDIBaseline, registered `"hdn_ddi"`) |
| `multi_cls/__init__.py` | 19 | ✓ |
| `multi_cls/baseline.py` | 367 | ✓ (HDNDDIMulticlassBaseline, registered `"hdn_ddi_mc"`) |
| `multi_cls/model.py` | 83 | ✓ (HDN_DDI_MC K-way head) |

### Reference paper / github (re-used from reproductions/ review)
See `Code/reproductions/HDN-DDI/_reviews/2026-05-17__paper_faithful.md` §1.
Full paper + jcsun-00 repo `drugbank_test/*` + `repeat.sh` + `evaluate.ipynb`
all read.

---

## 2. Tasks classification per CLAUDE.md §"从 reproduction 派生"

| Task | Same as paper task? | CLAUDE.md class | Implication |
|---|---|---|---|
| `binary_cls` | ✅ paper does per-triplet binary | "任务 X 与 paper 原始任务一致" | Must be ABSOLUTELY paper-faithful (allowed: data source = project's PairDataset, eval split = project's S0/S1/S2) |
| `multi_cls` | ❌ paper doesn't do K-way prediction | "任务 Y 是 paper 没做的任务" | Must PRESERVE paper core contributions; allowed task changes = K-way head, cross-entropy, top1/macro_f1 metrics |

---

## 3. Paper core contributions verification (contribution-first per CLAUDE.md)

For `binary_cls/baseline.py` (HDNDDIBaseline, registered `"hdn_ddi"`):

| # | Contribution | Implementation | Verified |
|---|---|---|---|
| 1 | refined BRICS substructure extraction | `mol_features.py:_refined_brics_fragment_atom_sets()` | ✅ aspirin → 4 frags, atorvastatin → 8 frags |
| 2 | 3-level hierarchical graph (atoms y=0 + frag y=1 + super y=2) | `mol_features.py:mol_to_data()` lines 310-340 | ✅ aspirin y unique = [0, 1, 2] |
| 3 | Bipartite y==1 substructure-only | `_shared.py:bipartite_edge_index_y1()` lines 51-62, called by `binary_cls/baseline.py:_make_pair_batch:172` | ✅ aspirin (4 frags) × atorvastatin (8 frags) = 32 bipartite edges (NOT 900 all-pairs) |
| 4 | 66-dim node features (Table S1) | `mol_features.py:ATOM_FEATURE_DIM = 66` | ✅ verified empirically |

For `multi_cls/baseline.py` (HDNDDIMulticlassBaseline, registered `"hdn_ddi_mc"`):

| # | Contribution | Inherited via | Verified |
|---|---|---|---|
| 1 | refined BRICS | `build_drug_graphs` import | ✅ `multi_cls/baseline.py:122` calls it |
| 2 | 3-level graph | same featurizer | ✅ |
| 3 | y==1 bipartite | inherits `HDNDDIBaseline._make_pair_batch` (NOT overridden) | ✅ `HDNDDIMulticlassBaseline._make_pair_batch is HDNDDIBaseline._make_pair_batch` (Python `is` check passes) |
| 4 | 66-dim features | inherits `in_features=ATOM_FEATURE_DIM` default | ✅ |
| 5 | K-way output head (task-relevant change) | `HDN_DDI_MC` einsum over rel_emb | ✅ `multi_cls/model.py:35-83` |
| 6 | cross-entropy loss (task-relevant change) | `F.cross_entropy(logits, y)` | ✅ `multi_cls/baseline.py:215` |
| 7 | top1_acc / macro_f1 metrics (task-relevant change) | `eval_multiclass()` from `my_code.utils.task_eval` | ✅ |

---

## 4. CLAUDE.md "派生 review 规范" 表格对照

| Dimension | reproductions/ paper-faithful | baseline/hdn_ddi/binary_cls | Allowed? |
|---|---|---|---|
| Model architecture | shared `models.py` | shared `models.py` (own copy) | ✅ |
| Input format | jcsun-00 BRICS pkl (1706 drugs) | project PairDataset (project's drug pool) | ✅ data source |
| Loss | SigmoidLoss = -logσ(p) + -logσ(-n) / 2 | `BCEWithLogits` (mathematically equivalent) | ✅ |
| Loss reduction | mean | mean | ✅ |
| Optimizer | Adam(lr=1e-3, wd=5e-4) | Adam(lr=1e-3, wd=5e-4) | ✅ |
| LR / weight_decay | 1e-3 / 5e-4 | 1e-3 / 5e-4 default | ✅ |
| LR scheduler | LambdaLR(0.96^ep) | LambdaLR(0.96^ep) | ✅ (was P0 fix earlier in round) |
| 训练协议 (per-epoch neg resample) | dynamic per-batch via DrugDataset | per-epoch via `regenerate=True` | ✅ (project API granularity = per-epoch, closest possible) |
| Best ckpt | paper uses s1+s2 mean (test-tuning) | val_s2 AUC | ⚪ allowed (honest hold-out > paper's test-tuning) |
| Eval 频率 | every epoch | configurable via `eval_strategy` | ✅ |
| Batch size | 512 | 512 default | ✅ |
| 评测指标 | ACC/AUROC/AUPR/F1 | ACC/AUROC/AUPR/F1 same | ✅ |

For `multi_cls`:
| Dimension | binary_cls (paper-faithful) | multi_cls (new task) | Allowed? |
|---|---|---|---|
| 输出头维度 | scalar (B,) | (B, K) softmax | ✅ task-relevant change |
| Loss | BCE | F.cross_entropy | ✅ |
| 主指标 | AUC | top1_acc | ✅ task-relevant change |
| Best ckpt | val_auc | val_top1 | ✅ |
| 负样本 | 需要 (binary) | 不需要 (multi-class) | ✅ |
| Optimizer/scheduler/architecture | same | same (inherits) | ✅ |

---

## 5. Layout + Independence

### Layout (CLAUDE.md §"Baseline 目录布局规范")
- ✅ Task subfolders: `binary_cls/`, `multi_cls/`
- ✅ Shared code at top level: `models.py`, `layers.py`, `mol_features.py`, `_shared.py`
- ✅ Top-level `__init__.py` re-exports both task variants
- ✅ Sub-`__init__.py` re-exports only its task

### Independence (CLAUDE.md §"文件级独立性")
- ✅ 0 `from reproductions` / `import reproductions` lines in `baseline/hdn_ddi/`
  (precise grep: only docstring mentions exist, which is encouraged for context)
- ✅ 0 `from baseline.hdn_ddi` lines in `reproductions/HDN-DDI/`
- ✅ Same-named files differ:
  ```
  layers.py     baseline=be25eb26e5  reproductions=24b328ebb2  drift=True
  models.py     baseline=464a2d22d8  reproductions=131a5aa03d  drift=True
  ```
- ✅ reproductions/HDN-DDI/ untouched during this migration (all timestamps May 16-17 from prior work, none from today's baseline work)

---

## 6. Migration history (this round)

User instruction: 删 flat 变体, 只保留 paper-faithful, suffix `_brics` 不再需要,
然后 apply P0 fixes.

### Deletions (3 flat-variant files)
- `binary_cls/baseline.py` (old flat version) → removed
- `mol_features.py` (old flat 55-dim) → removed
- `multi_cls/baseline.py` (old flat MC) → removed

### Renames (BRICS variants → drop `_brics` suffix)
- `binary_cls/baseline_brics.py` → `binary_cls/baseline.py`
- `mol_features_brics.py` → `mol_features.py`
- Class `HDNDDIBricsBaseline` → `HDNDDIBaseline`
- Constant `ATOM_FEATURE_DIM_BRICS` → `ATOM_FEATURE_DIM`
- Functions: `build_drug_graphs_brics` → `build_drug_graphs`, `mol_to_brics_data` → `mol_to_data`
- Registry `"hdn_ddi_brics"` → `"hdn_ddi"`

### Additions
- `_shared.py` (NEW): `drug_smiles_dict`, `bipartite_edge_index_y1` — moved here
  from `binary_cls/baseline.py` to comply with "shared code at top level"
- `multi_cls/baseline.py` (NEW; replaces old flat): BRICS-aware K-way variant
  inheriting `HDNDDIBaseline` with task-relevant overrides for `fit` /
  `predict_proba` / `save` / `load`

### P0 fixes applied (this round)
| Fix | File:line |
|---|---|
| `LambdaLR(0.96^epoch)` defined | `binary_cls/baseline.py:219`, `multi_cls/baseline.py:144` |
| `scheduler.step()` called per epoch | `binary_cls/baseline.py:330`, `multi_cls/baseline.py:258` |
| Per-epoch dynamic negatives (`regenerate=True`) | `binary_cls/baseline.py:266` |
| Stale docstring `"hdn_ddi_multiclass"` → `"hdn_ddi_mc"` | `multi_cls/__init__.py:9` (codex round-2 finding) |
| Dead imports removed (`make_bipartite_data` / `_bipartite_edge_index_y1` / `ATOM_FEATURE_DIM` / `Batch`) | `multi_cls/baseline.py:49-61` (codex round-2 finding) |

---

## 7. Codex independent verdict (final round)

After all migration + P0 fixes + dead-import cleanup, codex was called for a
final independent review. Verdict (verbatim):

> **PASS-with-minor-issues** (after fix → PASS)
>
> Findings:
> 1. `multi_cls/__init__.py:9` has a stale registry docstring → ✅ fixed
> 2. `multi_cls/baseline.py:54, 56, 60` dead imports → ✅ fixed
>
> Verified OK:
> - 66-dim features `mol_features.py:77`
> - 3-level y at `mol_features.py:310, 315, 317`
> - refined BRICS at `mol_features.py:231`
> - y==1-only bipartite at `_shared.py:56-57`
> - binary `_make_pair_batch` wires y==1 bipartite via `binary_cls/baseline.py:160, 172-173`
> - multi-class inherits the helper (does not override)
> - BCE loss in binary at `:297`
> - dynamic negatives `regenerate=True` at `:266`
> - LambdaLR + scheduler.step() both fit loops
> - Adam lr/wd correctly wired in both
> - TrainProgress / eval / save strategy support both
> - Registry keys only `"hdn_ddi"` + `"hdn_ddi_mc"`; no leftover `_brics` / `_flat`
> - 0 cross-imports between baseline and reproductions
> - same-named files physically independent (sha drift confirmed)

---

## 8. Final verdict: ✅ PASS

`baseline/hdn_ddi/` is paper-faithful for both tasks:
- binary_cls = absolutely faithful to paper algorithm (with allowed data-source
  + project-eval-split deviations)
- multi_cls = paper core contributions preserved (BRICS + 3-level + y==1
  bipartite + LambdaLR all inherited from binary_cls); only task-surface
  changes (K-way head, CE loss, top1/macro-F1 metrics)

---

## 9. Future work / unresolved

- [ ] End-to-end smoke train: run `"hdn_ddi"` baseline on project's PairDataset
      for a few epochs to confirm `fit() → predict_proba()` round-trip works
- [ ] Validate numerical sanity: HDN-DDI binary AUROC on project's S2 should be
      in a paper-comparable range (paper Ds2 AUROC ~96%, project's data may
      shift this materially due to different drug pool / split protocol)
- [ ] Consider whether `binary_cls/baseline.py` `_make_pair_batch` should
      eventually move some logic into `_shared.py` (currently
      `multi_cls/baseline.py` inherits it — that's fine, but if we add a third
      task variant it might become cleaner to share at top level)
- [ ] `n_epochs=5` default is project's "smoke" default; production runs must
      override (already documented; just flagging)
